{
  "name": "arista-7050qx-dashboard-preview",
  "version": "2.0.0",
  "private": true,
  "description": "Local fixture preview and regression harness for the Arista on-box dashboard.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "preview": "node server.js",
    "build:web": "python tools/build_onbox.py",
    "check:web": "node --check web/app.js && node --check server.js && python tools/build_onbox.py --check",
    "test": "python -m unittest discover -s tests -v",
    "test:e2e": "playwright test",
    "test:frontend": "node --check web/app.js && node --check server.js && playwright test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
